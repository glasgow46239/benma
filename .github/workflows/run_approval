- name: Run approval trackers
  run: |
    for dir in trackers/pm-approval/; do
      python pipeline/approval.py --config "${dir}config.json" --push-to-datawrapper
    done
  env:
    DATAWRAPPER_TOKEN: ${{ secrets.DATAWRAPPER_TOKEN }}
